# Directory Description

The `data` directory contains pre-compiled encoded FORTH programs,
with pickled codebooks and associative memories for recall in interpretation.

For more information about the caching of values, see the `../forth`
directory and associated modules. In particular, see `../forth/encoding.py`.